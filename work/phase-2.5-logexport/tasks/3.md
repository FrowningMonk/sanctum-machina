---
status: planned
depends_on: []
wave: 1
skills: [code-writing]
verify: [smoke]
reviewers: [code-reviewer, security-auditor, test-reviewer]
teammate_name:
---

# Task 3: LogExportManager + DeviceInfoCollector + LogcatReader + TapCounter

## Required Skills

Перед выполнением задачи загрузи:
- `/skill:code-writing` — [skills/code-writing/SKILL.md](~/.claude/skills/code-writing/SKILL.md)

## Description

Реализовать четыре класса под `app/src/main/kotlin/app/sanctum/machina/logexport/` — ядро сборки диагностического `.txt`-экспорта для фазы 2.5:

1. **`LogExportManager`** (`@Singleton`) — собирает финальный текстовый отчёт (header + `crash.log` + `errors.log` + `errors.log.1` + `logcat`) в одну `String` на `Dispatchers.IO` и умеет писать этот `String` в выбранный пользователем SAF-URI. Каждая секция имеет свой cap согласно Decision 7 (`crash.log` head-truncated ≤100 KB, `logcat` tail-truncated ≤100 KB, `errors.log`/`errors.log.1` ≤2 MB каждая — уже гарантировано ротацией `ErrorLog`, header ≤~1 KB). Если секция обрезана — дописывается маркер `[truncated at NNN KB]`. Отсутствующие/пустые источники рендерятся как `[empty]`; отсутствующий `errors.log.1` — секция опускается целиком (без пустого `=== errors.log.1 ===` разделителя).
2. **`DeviceInfoCollector`** — формирует текстовый header по фиксированному шаблону (applicationId, version + debug flag, manufacturer/model, Android version + API level, total/available RAM, active model id либо `none`, downloaded models list либо `(none)`). Зависимости (`PackageInfo`, `ActivityManager`, `Build`-константы) берутся через собственный интерфейс-провайдер, чтобы юнит-тесты могли подсовывать детерминированный stub.
3. **`LogcatReader`** — читает собственный лог процесса через `ProcessBuilder("logcat", "-d", "-v", "threadtime", "--pid=${Process.myPid()}", "*:E").redirectErrorStream(true)`. Чтобы обойти 64-KB deadlock pipe-буфера, stdout вычитывается в отдельном worker-потоке параллельно с `waitFor(2, SECONDS)`; на таймаут — `destroy()`. Возвращает raw-байты (обрезку выполняет `LogExportManager`) либо placeholder `[logcat unavailable: <reason>]`. Для тестов — seam `CommandRunner`.
4. **`TapCounter`** — pure JVM (никаких Android-зависимостей), 7-tap детектор: 7 касаний с промежутками ≤2 с подряд → срабатывает ровно один раз на 7-м касании; разрыв >2 с → счётчик сбрасывается в 1. Время берётся через функциональный параметр `nowNanos: () -> Long` для тестирования без моков.

Задача — ядро Wave 1 фичи фазы 2.5. Wave 2 (CrashReportActivity, AboutScreen, ModelManagerScreen banner) напрямую зависит от этих четырёх классов. Относится к Decisions 1 (single `.txt`), 7 (section caps + direction), 8 (logcat source-aware), 11 (no filtering/redaction).

## What to do

1. **Создать package `app/src/main/kotlin/app/sanctum/machina/logexport/`** — четыре новых файла + их тестовые двойники под `app/src/test/kotlin/app/sanctum/machina/logexport/`.

2. **`TapCounter.kt`** — pure-JVM класс без Android-импортов. Публичный API: конструктор принимает `nowNanos: () -> Long`; метод `tap(): Boolean` возвращает `true` только на 7-м касании в пределах допустимых окон. Граничное условие — inclusive ≤2 с (ровно 2_000_000_000 нс от предыдущего касания считается «в окне»), 2 с + 1 нс — reset. После срабатывания (7-й tap → `true`) счётчик обнуляется: 8-й tap возвращает `false` и начинает новый цикл с 1.

3. **`LogcatReader.kt`** — класс + `interface CommandRunner` с методом, принимающим `argv: List<String>` и `timeoutMs: Long` и возвращающим структуру с `exitCode: Int?`, `stdout: ByteArray`, `timedOut: Boolean`. Прод-реализация обёрнута в `DefaultCommandRunner`, стартует `ProcessBuilder` с ровно шестью аргументами `["logcat", "-d", "-v", "threadtime", "--pid=${Process.myPid()}", "*:E"]`, `redirectErrorStream(true)`, читает stdout в фоновом `Thread`/executor'е, `process.waitFor(2, TimeUnit.SECONDS)` — при `false` → `process.destroy()` → timeout. `LogcatReader.read(): String` возвращает: raw UTF-8 stdout → если stdout пустой → `[logcat unavailable: empty]`; timeout → `[logcat unavailable: timeout]`; non-zero exit → `[logcat unavailable: exit=N]`; иначе — raw output дословно (обрезку делает `LogExportManager`).

4. **`DeviceInfoCollector.kt`** — класс + `interface DeviceInfoProvider` (методы: `applicationId()`, `versionName()`, `versionCode()`, `isDebug()`, `manufacturer()`, `model()`, `androidRelease()`, `apiLevel()`, `totalMemoryBytes()`, `availableMemoryBytes()`, `activeModelId(): String?`, `downloadedModels(): List<Pair<String, Long>>` — id + size bytes, либо аналогичный чистый snapshot). `DeviceInfoCollector.buildHeader(): String` рендерит блок по шаблону из tech-spec «Data Models». Прод-реализация `AndroidDeviceInfoProvider` внедряется через конструктор и использует `PackageInfo` + `ActivityManager.MemoryInfo` + `Build` (без статических обращений изнутри коллектора, чтобы тесты могли подставить stub без Robolectric).

5. **`LogExportManager.kt`** — `@Singleton class LogExportManager @Inject constructor(@ApplicationContext private val context: Context, private val deviceInfo: DeviceInfoCollector, private val logcat: LogcatReader)` (точные сигнатуры уточнить при реализации, но конструктор Hilt-friendly и с DI-seam'ами для тестов). Предусмотреть non-Hilt путь инстанцирования для использования в `:crash` процессе из Task 4 (Decision 5) — либо secondary constructor, либо companion-factory. API:
    - `enum class ExportSource { About, CrashReport }`.
    - `suspend fun buildExport(source: ExportSource): String` — всё на `withContext(Dispatchers.IO)`, собирает в `StringBuilder` header (без cap, формально ≤1 KB) → `\n\n=== crash.log ===\n` → content/`[empty]` → `\n\n=== errors.log ===\n` → content/`[empty]` → `\n\n=== errors.log.1 ===\n` → content (или пропустить блок если файла нет) → `\n\n=== logcat ===\n` → `CrashReport` даёт `[logcat available only via About export]` (Decision 8), `About` — `logcat.read()` с tail-truncate до 100 KB.
    - `suspend fun writeTo(uri: Uri, content: String)` — `context.contentResolver.openOutputStream(uri).use { out -> ... }`; если `openOutputStream` вернул `null` — явно `throw IOException("openOutputStream returned null")`. `IOException` пробрасывается наружу (вызывающий код в Wave 2 показывает Toast/Snackbar).
    - **Truncation:** `crash.log` — head-truncate (берём первые 100 KB — стектрейс + тип исключения в начале), в конец дописываем `\n[truncated at 100 KB]`. `logcat` — tail-truncate (берём последние 100 KB — наиболее свежие строки), в начало дописываем `[truncated: head NNN bytes]\n`. Маркеры появляются **только** если обрезка реально сработала.
    - **Fresh-install guard:** если `filesDir/logs/` не существует — не бросать, все секции рендерятся как `[empty]`/пропущенные без `FileNotFoundException`.

6. **Тесты** — mirror Robolectric-паттерн из `core-runtime/src/test/kotlin/app/sanctum/machina/core/log/ErrorLogTest.kt` (JUnit4 + `@RunWith(RobolectricTestRunner::class)` + `@Config(sdk = [33])` + `ApplicationProvider.getApplicationContext()` + явные `@Before setUp()` / `@After tearDown()` с `logFile.parentFile?.deleteRecursively()`). `TapCounterTest` — без Robolectric, обычный JUnit4.

## TDD Anchor

Тесты пишутся ДО реализации. Запустить → убедиться что падают → реализовать → убедиться что проходят.

- `app/src/test/kotlin/app/sanctum/machina/logexport/TapCounterTest.kt::sevenTaps_withinWindow_triggersOnSeventh` — 7 tap'ов с виртуальными временами 0, 1 с, ..., 6 с; первые 6 возвращают `false`, 7-й — `true`
- `TapCounterTest::gapOverTwoSeconds_resetsCounter` — tap, gap >2 с, tap → второй возвращает `false` (счётчик = 1), не `true`
- `TapCounterTest::gapExactlyTwoSeconds_inclusive` — gap ровно 2_000_000_000 нс → попадает в окно (считается продолжением последовательности)
- `TapCounterTest::gapTwoSecondsPlusOneNs_exclusive` — gap 2_000_000_001 нс → reset, счётчик = 1
- `TapCounterTest::fewerThanSeven_noTrigger` — 6 tap'ов в окне → ни разу не `true`
- `TapCounterTest::eighthTapDoesNotRetrigger` — 7 tap'ов → `true` на 7-м; 8-й tap (в окне от 7-го) возвращает `false` (срабатывание один раз за цикл)
- `app/src/test/kotlin/app/sanctum/machina/logexport/LogcatReaderTest.kt::emptyStdout_returnsUnavailableEmpty` — stub runner возвращает пустой stdout, exit=0 → `[logcat unavailable: empty]`
- `LogcatReaderTest::nonZeroExit_returnsUnavailableExitN` — exit=13 → `[logcat unavailable: exit=13]`
- `LogcatReaderTest::timeout_returnsUnavailableTimeout` — stub помечает `timedOut = true` → `[logcat unavailable: timeout]`
- `LogcatReaderTest::happyPath_returnsStdoutVerbatim` — stub выдаёт фиксированную строку → reader возвращает её без изменений (tail-truncate — ответственность `LogExportManager`, не reader'а)
- `LogcatReaderTest::argvShape_exactlySixArgs_knownPositions` — recording runner фиксирует argv; `assertEquals(6, argv.size)`, `argv[0] == "logcat"`, `argv[1] == "-d"`, `argv[2] == "-v"`, `argv[3] == "threadtime"`, `argv[4]` соответствует `^--pid=\d+$`, `argv[5] == "*:E"`. Guardrail для Decision 8 + OWASP A03
- `app/src/test/kotlin/app/sanctum/machina/logexport/DeviceInfoCollectorTest.kt::headerFormatting_deterministicFromStub` — provider отдаёт фиксированные значения → `buildHeader()` возвращает exact-match строку по шаблону из tech-spec Data Models
- `DeviceInfoCollectorTest::emptyDownloadedModels_rendersNonePlaceholder` — `downloadedModels()` пустой → в рендере `downloaded models:` + `  (none)` (а не trailing colon с пустотой)
- `DeviceInfoCollectorTest::nullActiveModelId_rendersNone` — `activeModelId() == null` → `active model: none`
- `app/src/test/kotlin/app/sanctum/machina/logexport/LogExportManagerTest.kt::headerContainsRequiredFields` — в выводе `buildExport(About)` присутствуют substring'и: `applicationId:`, `version:`, `device:`, `memory:`, `active model:`, `downloaded models:`
- `LogExportManagerTest::allSectionsPresent_orderedCorrectly` — seed'нуть 4 файла + stubbed logcat с distinctive маркерами; `indexOf("=== crash.log ===") < indexOf("=== errors.log ===") < indexOf("=== errors.log.1 ===") < indexOf("=== logcat ===")`, каждый маркер в своей секции
- `LogExportManagerTest::missingErrorsLog_rendersEmpty` — нет `errors.log` → секция содержит `[empty]`
- `LogExportManagerTest::missingCrashLog_rendersEmpty` — нет `crash.log` → `[empty]`
- `LogExportManagerTest::crashReportSource_logcatPlaceholder` — `buildExport(CrashReport)` → секция `logcat` содержит ровно `[logcat available only via About export]`, `LogcatReader` не вызывается (stub может бросать — тест убедится, что он нетронут)
- `LogExportManagerTest::aboutSource_logcatPopulatedFromRunner` — `buildExport(About)` со stubbed `CommandRunner` возвращающим `"sentinel-logcat-content"` → эта строка присутствует в секции logcat
- `LogExportManagerTest::logcatTailTruncation_preservesTailAndMarks` — stub отдаёт 200 KB с distinctive концовкой; в выводе последние ~100 KB совпадают с хвостом входа И в начале секции присутствует маркер `[truncated: head ... bytes]`
- `LogExportManagerTest::crashLogHeadTruncation_preservesHeadAndMarks` — seed 200 KB `crash.log` с distinctive началом; в выводе начальные ~100 KB совпадают с головой входа И в конце секции присутствует маркер `[truncated at NNN KB]`
- `LogExportManagerTest::errorsLog1Absent_sectionOmitted` — `errors.log.1` не существует → в выводе нет подстроки `=== errors.log.1 ===` вообще
- `LogExportManagerTest::freshInstall_noLogsDir_succeeds` — `filesDir/logs/` не существует → `buildExport(About)` возвращает header + все секции как `[empty]`, не бросает
- `LogExportManagerTest::writeTo_ioException_surfaces` — `ShadowContentResolver` принуждает `openOutputStream(uri)` бросить `IOException` → `writeTo` пробрасывает `IOException` наружу
- `LogExportManagerTest::writeTo_nullOutputStream_surfacesAsIoException` — shadow возвращает `null` из `openOutputStream(uri)` → `writeTo` бросает `IOException("openOutputStream returned null")`
- `LogExportManagerTest::emptyDownloadedModels_rendersBlankList` — header когда registry отдаёт zero downloaded models содержит `(none)`, не trailing colon с пустотой

Правила: никаких Mockito/MockK — hand-rolled fakes (см. `ErrorLogTest.kt` стиль); все тестовые файлы чистят `filesDir/logs` в `@Before` и `@After`.

## Acceptance Criteria

- [ ] Четыре продовых файла созданы под `app/src/main/kotlin/app/sanctum/machina/logexport/`: `LogExportManager.kt`, `DeviceInfoCollector.kt`, `LogcatReader.kt`, `TapCounter.kt`.
- [ ] Четыре тестовых файла созданы под `app/src/test/kotlin/app/sanctum/machina/logexport/`: `LogExportManagerTest.kt`, `DeviceInfoCollectorTest.kt`, `LogcatReaderTest.kt`, `TapCounterTest.kt`.
- [ ] `./gradlew :app:test --tests "*LogExportManagerTest*" --tests "*DeviceInfoCollectorTest*" --tests "*LogcatReaderTest*" --tests "*TapCounterTest*"` — all green (все именованные кейсы из TDD Anchor присутствуют и проходят).
- [ ] `LogcatReader` использует ровно 6-аргументный argv через `ProcessBuilder`, без shell-обёрток и без интерполяции произвольных строк — argv-shape-тест зелёный.
- [ ] `LogExportManager.buildExport` работает на `Dispatchers.IO` (есть `withContext(Dispatchers.IO)` в коде).
- [ ] `ExportSource.CrashReport` не вызывает `LogcatReader` (тест это подтверждает).
- [ ] `TapCounter.kt` не содержит ни одного Android-импорта.
- [ ] `LogExportManager`, `DeviceInfoCollector`, `LogcatReader` не вызывают `ErrorLog.e` (Decision 10: канал `crash.log` отдельно от `ErrorLog`).
- [ ] `./gradlew :core-runtime:test` остаётся зелёным (не задеты whitelist'ы `ErrorLog`).
- [ ] Никаких новых внешних зависимостей в `app/build.gradle.kts` или `gradle/libs.versions.toml`.

## Context Files

**Фича-специфичные:**
- [user-spec.md](../user-spec.md)
- [tech-spec.md](../tech-spec.md)
- [decisions.md](../decisions.md)

**Проектные (project-knowledge):**
- [project.md](../../../.claude/skills/project-knowledge/references/project.md)
- [architecture.md](../../../.claude/skills/project-knowledge/references/architecture.md)
- [patterns.md](../../../.claude/skills/project-knowledge/references/patterns.md)

**Код (существующие файлы — прочитать для контекста):**
- [core-runtime/src/main/kotlin/app/sanctum/machina/core/log/ErrorLog.kt](../../../core-runtime/src/main/kotlin/app/sanctum/machina/core/log/ErrorLog.kt) — формат существующего `errors.log` и ротация (2 MB cap), стиль Hilt `@Singleton @Inject class`, `suspend + withContext(Dispatchers.IO)` паттерн
- [core-runtime/src/test/kotlin/app/sanctum/machina/core/log/ErrorLogTest.kt](../../../core-runtime/src/test/kotlin/app/sanctum/machina/core/log/ErrorLogTest.kt) — Robolectric-паттерн (JUnit4 + `@Config(sdk = [33])` + `ApplicationProvider` + `deleteRecursively` cleanup) — референс для всех новых Robolectric-тестов
- [app/build.gradle.kts](../../../app/build.gradle.kts) — `:app` модуль: Hilt подключён, Compose ON, `minSdk=31/targetSdk=35`, `testOptions.unitTests.isIncludeAndroidResources = true` уже активно, Robolectric + `androidx.test.core` + `kotlinx-coroutines-test` + JUnit4 в `testImplementation` — всё нужное есть, новых зависимостей не требуется

**Код (новые файлы — создать в этой задаче):**
- `app/src/main/kotlin/app/sanctum/machina/logexport/LogExportManager.kt`
- `app/src/main/kotlin/app/sanctum/machina/logexport/DeviceInfoCollector.kt`
- `app/src/main/kotlin/app/sanctum/machina/logexport/LogcatReader.kt`
- `app/src/main/kotlin/app/sanctum/machina/logexport/TapCounter.kt`
- `app/src/test/kotlin/app/sanctum/machina/logexport/LogExportManagerTest.kt`
- `app/src/test/kotlin/app/sanctum/machina/logexport/DeviceInfoCollectorTest.kt`
- `app/src/test/kotlin/app/sanctum/machina/logexport/LogcatReaderTest.kt`
- `app/src/test/kotlin/app/sanctum/machina/logexport/TapCounterTest.kt`

## Verification Steps

### Automated
- `./gradlew :app:test --tests "*TapCounterTest*"` → green
- `./gradlew :app:test --tests "*LogcatReaderTest*"` → green
- `./gradlew :app:test --tests "*DeviceInfoCollectorTest*"` → green
- `./gradlew :app:test --tests "*LogExportManagerTest*"` → green
- `./gradlew :core-runtime:test` → green (регрессия-гейт для `ErrorLog`)

### Smoke
- `./gradlew :app:test --tests "*LogExportManagerTest*" --tests "*DeviceInfoCollectorTest*" --tests "*LogcatReaderTest*" --tests "*TapCounterTest*"` → all green (прямой smoke из tech-spec Task 3 `Verify-smoke`).

## Details

**Files:**

- `app/src/main/kotlin/app/sanctum/machina/logexport/LogExportManager.kt` — **создать**. `@Singleton class LogExportManager @Inject constructor` (Context via `@ApplicationContext`, `DeviceInfoCollector`, `LogcatReader`). Методы `suspend buildExport(source: ExportSource): String` и `suspend writeTo(uri: Uri, content: String)`. Enum `ExportSource { About, CrashReport }` — в том же файле или рядом top-level. Все секции собираются через `StringBuilder`, I/O через `withContext(Dispatchers.IO)`. Truncation: `crash.log` — head-slice до 100 KB + суффикс `\n[truncated at 100 KB]`; `logcat` — tail-slice до 100 KB + префикс `[truncated: head NNN bytes]\n`. Маркеры добавляются только если реальная обрезка произошла. `errors.log.1` отсутствует → блок `=== errors.log.1 ===` + контент опускается целиком. `writeTo` использует `context.contentResolver.openOutputStream(uri)?.use { it.write(content.toByteArray(Charsets.UTF_8)) } ?: throw IOException("openOutputStream returned null")`. Для Task 4 (non-Hilt в `:crash` процессе) предусмотреть secondary constructor или companion factory — точная форма на усмотрение реализатора, но без дублирования логики сборки.

- `app/src/main/kotlin/app/sanctum/machina/logexport/DeviceInfoCollector.kt` — **создать**. `class DeviceInfoCollector(private val provider: DeviceInfoProvider)` + `interface DeviceInfoProvider`. `buildHeader(): String` — детерминированный рендер по формату из tech-spec «Data Models». Прод-реализация `AndroidDeviceInfoProvider @Inject constructor(@ApplicationContext context)` обращается к `context.packageManager.getPackageInfo(context.packageName, 0)`, `context.getSystemService(ActivityManager::class.java)` → `MemoryInfo`, `Build.MANUFACTURER` / `Build.MODEL` / `Build.VERSION.RELEASE` / `Build.VERSION.SDK_INT`. `activeModelId()` и `downloadedModels()` — на данном этапе допустимо возвращать заглушки (`null` / empty list) с TODO-комментарием для последующей интеграции с ModelRegistry (это не блокирует Task 3 в Wave 1, но должно быть помечено явно в коде). Если коллектору нужен доступ к реальному registry — передать коллаборатор через конструктор, а не статически.

- `app/src/main/kotlin/app/sanctum/machina/logexport/LogcatReader.kt` — **создать**. `class LogcatReader @Inject constructor(private val runner: CommandRunner)` + `interface CommandRunner { fun run(argv: List<String>, timeoutMs: Long): LogcatResult }`. `data class LogcatResult(val stdout: ByteArray, val exitCode: Int?, val timedOut: Boolean)`. Прод-реализация `DefaultCommandRunner` собирает `ProcessBuilder(argv).redirectErrorStream(true)`, запускает, в отдельном worker-потоке читает `process.inputStream` в `ByteArrayOutputStream` до EOF, параллельно вызывает `process.waitFor(timeoutMs, MILLISECONDS)`. Если `waitFor == false` → `process.destroy()`; дождаться worker'а с небольшим доп-таймаутом и установить `timedOut = true`, `exitCode = null`. Public `fun read(): String` собирает argv как `listOf("logcat", "-d", "-v", "threadtime", "--pid=${Process.myPid()}", "*:E")`, вызывает runner с timeout=2000, возвращает строку согласно правилам (см. What to do п.3). Hilt-binding `DefaultCommandRunner` — через отдельный минимальный `@Module @InstallIn(SingletonComponent::class) object LogExportModule` в том же package, если Hilt не разрешает интерфейс автоматически.

- `app/src/main/kotlin/app/sanctum/machina/logexport/TapCounter.kt` — **создать**. `class TapCounter(private val nowNanos: () -> Long, private val maxGapNanos: Long = 2_000_000_000L, private val threshold: Int = 7)`. Внутреннее состояние: `private var count: Int = 0; private var lastTapNanos: Long = 0L`. Метод `fun tap(): Boolean`: взять `now = nowNanos()`; если `count == 0` или `now - lastTapNanos > maxGapNanos` → `count = 1`; иначе `count++`. `lastTapNanos = now`. Если `count == threshold` → сбросить `count = 0` и вернуть `true`; иначе `false`. Никакой синхронизации — вызов с UI-потока, однопоточный контракт (задокументировать KDoc). **Нет `import android.*`** — класс pure JVM.

- Тесты — mirror `ErrorLogTest.kt`. `@RunWith(RobolectricTestRunner::class) @Config(sdk = [33])` для Robolectric-тестов, `@Before setUp() { context = ApplicationProvider.getApplicationContext(); logDir = File(context.filesDir, "logs"); logDir.deleteRecursively() }`, `@After tearDown() { logDir.deleteRecursively() }`. `runTest { ... }` из `kotlinx-coroutines-test` для suspend-функций. `TapCounterTest` — без `@RunWith`, обычный JUnit4, виртуальное время через `var t = 0L; val counter = TapCounter(nowNanos = { t })`. Никаких Mockito/MockK — hand-rolled fakes (stub-провайдер для `DeviceInfoProvider`, `RecordingCommandRunner` для `CommandRunner`).

**Dependencies:**
- Ничего нового в `libs.versions.toml` / `app/build.gradle.kts`. Всё необходимое уже на classpath: Hilt, `activity.compose`, Robolectric, `androidx.test.core`, `kotlinx.coroutines.test`, JUnit4.
- Task 3 независим в Wave 1 (`depends_on: []`), но параллельный Task 2 пишет `crash.log` на диск, а Task 3 читает тот же `filesDir/logs/crash.log`. Контрактная связь — только через имя файла и формат (tech-spec «Data Models»), без живых объектов. Wave 2 (Task 4 `CrashReportActivity`, Task 6 `AboutScreen`, Task 7 `ModelManagerScreen`) — потребители Task 3.

**Edge cases:**
- `filesDir/logs/` не существует (fresh install или после `CrashState.clear()` в параллельной сессии) — `buildExport` не бросает, все секции `[empty]`, без `FileNotFoundException`.
- `crash.log` / `errors.log` нулевого размера (файл есть, но пустой) — рендерится как `[empty]`, а не как пустая секция с голым разделителем.
- `errors.log.1` отсутствует — **секция пропускается целиком** (ни разделителя, ни placeholder'а) — отличается от `crash.log` / `errors.log`, которые всегда рендерятся с `[empty]`. Это явное требование tech-spec.
- Очень большой `crash.log` (>100 KB) — head-truncate до 100 KB + маркер. Очень большой logcat → tail-truncate до 100 KB + маркер. `errors.log` уже ≤2 MB по инварианту ротации в `ErrorLog`, доп-защиту не ставим — доверяем инварианту (tech-spec Decision 7).
- `ProcessBuilder("logcat", ...).start()` кидает `IOException` на некоторых OEM (paranoid mode) — `DefaultCommandRunner` должен это поймать и вернуть `LogcatResult(stdout=ByteArray(0), exitCode=null, timedOut=false)` → в итоге `[logcat unavailable: empty]` (или отдельный reason — **не пробрасывать IOException** наружу).
- Pipe-buffer deadlock: stdout >64 KB до `waitFor` — если читать **после** `waitFor`, процесс заблокируется на write. Поэтому чтение — **в отдельном потоке**, стартует **до** `waitFor`.
- SAF URI может указывать на удалённый провайдер, который закроет `OutputStream` асинхронно — `.use { }` гарантирует корректный cleanup, `writeTo` не ловит `IOException` (передаёт вызывающему).
- `Process.myPid()` в `:crash` процессе даёт pid нового процесса → logcat для него будет пустым (Decision 8: поэтому `CrashReport` ветка вообще не вызывает logcat).
- Boundary тест `TapCounter`: 2 с ровно — inclusive, 2 с + 1 нс — exclusive. Гарантирует стабильность семантики к возможному будущему переходу на ms.

**Implementation hints:**
- Формат header'а — см. tech-spec «Data Models» (строки `applicationId: ...`, `version: 0.1.0 (1), debug=true`, `device: Honor / HONOR 200 / Android 14 (API 34)`, `memory: total=11.7 GB, available=6.2 GB`, список моделей с отступом `  - <id> (<size>)`). Размер памяти форматировать в GB с одним знаком после точки через ОДНУ helper-функцию, чтобы `DeviceInfoCollectorTest` мог проверить exact-match.
- Section dividers: **ровно** `=== crash.log ===` (без лидирующих/trailing символов), между секциями — одна пустая строка (`"\n\n"`).
- Tail-truncate logcat: работаем в байтах до последнего шага; для byte→String декодирования при обрезке посреди UTF-8 multibyte-символа использовать `String(bytes, Charsets.UTF_8)` (JVM заменит битые начальные байты на `?`) или `CharsetDecoder` с `CodingErrorAction.REPLACE` — задокументировать выбор в KDoc.
- Head-truncate `crash.log`: аналогично — `bytes.copyOfRange(0, 100 * 1024)` + декодирование в UTF-8.
- `@Singleton` + `@Inject constructor` — Hilt автоматически подхватит class-level bindings; `@Provides`-модуль нужен только для интерфейса `CommandRunner` (прод-биндинг на `DefaultCommandRunner`). `LogExportModule` — минимальный, в том же package.
- `CommandRunner` — обычный `interface`, не `fun interface`, чтобы `RecordingCommandRunner` мог хранить state (`var lastArgv: List<String>?`).
- Следуй стилю `ErrorLog.kt` — `suspend` + `withContext(Dispatchers.IO)` вокруг ВСЕХ файловых операций, никаких блокирующих операций на caller-потоке.

**Security (OWASP A03 гвард):**
- `ProcessBuilder` принимает **list** аргументов, не строку — shell-интерполяция невозможна by design. Тест `argvShape_exactlySixArgs_knownPositions` закрепляет эту инвариантность.
- `--pid=${Process.myPid()}` — `myPid()` возвращает `Int`, интерполяция безопасна; формально `argv[4].matches(Regex("^--pid=\\d+$"))` это и ловит.
- `LogExportManager.writeTo(uri, content)` не логирует content и не валидирует URI — это контракт SAF, URI выбирает пользователь (Decision 11 явно принимает non-redaction).

## Reviewers

- **code-reviewer** → `work/phase-2.5-logexport/logs/working/task-3/code-reviewer-{round}.json`
- **security-auditor** → `work/phase-2.5-logexport/logs/working/task-3/security-auditor-{round}.json`
- **test-reviewer** → `work/phase-2.5-logexport/logs/working/task-3/test-reviewer-{round}.json`

## Post-completion

- [ ] Записать краткий отчёт в `work/phase-2.5-logexport/decisions.md` по шаблону (Summary: 1-3 предложения; все раунды ревью со ссылками на JSON; без таблиц файндингов и дампов)
- [ ] Если отклонились от tech-spec (формат header'а, сигнатуры классов, семантика truncation-маркера) — описать отклонение и причину
- [ ] Обновить user-spec / tech-spec если что-то реально изменилось в контракте (например, добавлено новое значение `[logcat unavailable: <reason>]`, которого не было в спеке)
