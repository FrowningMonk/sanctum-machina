---
status: planned
depends_on: [6, 7]
wave: 4
skills: [code-writing]
verify: []
reviewers: [code-reviewer, security-auditor, test-reviewer]
teammate_name:
---

# Task 8: ChatViewModel rework

## Required Skills

Перед выполнением задачи загрузи:
- `/skill:code-writing` — [SKILL.md](~/.claude/skills/code-writing/SKILL.md)

## Description

`ChatViewModel` — центральный класс для всех режимов чата (Quick / Draft / Persistent). Сейчас он тесно завязан на `modelName` как идентификатор и самостоятельно вызывает `registry.initialize(modelName)` при инициализации. В Phase 3 это неприемлемо по двум причинам:

1. **Прогрев engine — ответственность `WarmupCoordinator`**, а не VM. Если VM вызывает `initialize` в `init {}`, то открытие чата уничтожает смысл фонового warmup и создаёт гонки с `WarmupCoordinator`.
2. **`onCleared()` убивает прогретый движок** — вызов `registry.cleanup(modelName)` при каждом Back означает, что warmup умирает при первом же уходе из чата (AC-E6, Decision 5).

Задача: переписать `ChatViewModel` вокруг `ChatIdentity` (sealed class Quick / Draft / Persistent), заменить imperative `init {}` на реактивный `registry.models.collect {}`, добавить Room-backed message flow для persistent-чатов, реализовать draft→committed atomic transition, и удалить `onCleared()` cleanup.

`ChatViewModelTest` необходим для проверки ключевых инвариантов до интеграции с UI-слоем (Tasks 9–11). UI-level верификация поведения VM деферирована на Tasks 9–11, поэтому `verify: []`.

## What to do

1. **Введи `ChatIdentity` sealed class** в пакете `app.sanctum.machina.ui.chat` (или `app.sanctum.machina.ui.model` — на усмотрение, но рядом с `ChatViewModel`):
   - `object Quick` — route `chat/quick`
   - `object Draft` — route `chat/draft`
   - `data class Persistent(val id: Long)` — route `chat/{chatId}`

2. **Замени `modelName` на `ChatIdentity`** как primary identity в `ChatViewModel`. Читается из `SavedStateHandle`: если есть `chatId: Long` nav arg → `Persistent(id)`; если есть `kind = "draft"` → `Draft`; иначе → `Quick`. Константы nav args переименуй: `NAV_ARG_MODEL_NAME` → `NAV_ARG_CHAT_ID`, `NAV_ARG_KIND`.

3. **Замени `init {} registry.initialize(...)` на реактивный observer** `registry.models.collect {}`:
   - Для каждого `ModelEntry` подходящей модели (`chat.model_id` для Persistent, текущая прогретая для Quick/Draft) смотри `initStatus` и обновляй `_uiState` реактивно.
   - `ModelInitStatus.Ready` → `ChatUiState.Ready(isGenerating = false)` (сохраняя `isGenerating` если уже идёт стрим)
   - `ModelInitStatus.Initializing` → `ChatUiState.Loading`
   - `ModelInitStatus.Failed(msg)` → `ChatUiState.Failed(msg)`
   - `ModelInitStatus.Idle` / null → `ChatUiState.Loading` (ожидание; пользователь нажмёт «Загрузить»)
   - VM не вызывает `registry.initialize(...)` автоматически — warmup — задача `WarmupCoordinator`.

4. **Добавь Room-backed message flow** для `Persistent` режима:
   - `persistedMessages: Flow<List<MessageEntity>>` = `messageDao.observeByChat(chatId)`, коллектируется в `viewModelScope`.
   - `_streamingMessage: MutableStateFlow<Message?>` — in-memory streaming ASSISTANT bubble.
   - `displayMessages: StateFlow<List<Message>>` = `combine(persistedMessages, _streamingMessage)` через `.stateIn(viewModelScope, SharingStarted.Eagerly, emptyList())`.
   - **Atomic handover при `done=true`**: когда `persistedMessages` первый раз эмитит список с новой ASSISTANT-строкой (той, что только что была записана в Room) — сбрасывай `_streamingMessage = null` в том же `mapLatest`/`transformLatest` блоке. Цель: не допустить emission где и in-memory, и persisted ASSISTANT видны одновременно.
   - Quick и Draft режимы: `persistedMessages` не используется, `_messages` (in-memory) остаётся как в Phase 2.

5. **Реализуй draft→committed atomic transition**:
   - При первом `send()` в `Draft` режиме: вызвать `chatRepository.commitDraftChat(modelId, firstMessage, stagingDir)`.
   - После успешного commit: `navController.navigate("chat/$chatId") { popUpTo("chat/draft") { inclusive = true } }`.
   - При failure: rollback уже описан в `ChatRepository` (Task 4) — VM остаётся на draft, вложения в `_attachments` сохраняются для retry.

6. **Удали `onCleared()` cleanup** — весь блок `CoroutineScope(SupervisorJob() + Dispatchers.IO).launch { registry.cleanup(modelName) }`. После удаления `onCleared` сводится к `super.onCleared()` или вовсе отсутствует.

7. **Напиши `ChatViewModelTest`** в `app/src/test/kotlin/app/sanctum/machina/ui/chat/ChatViewModelTest.kt` (создай файл):
   - Quick lifecycle: VM не вызывает `MessageDao` ни разу.
   - Draft→Persistent promotion: при первом send в Draft вызывается `chatRepository.commitDraftChat(...)`.
   - `onCleared` не вызывает `registry.cleanup(...)`.
   - Engine state transitions: `registry.models` emitting `Ready` → `uiState` becomes `Ready`; `Failed` → `uiState` becomes `Failed`.
   - Double-bubble handover: нет emission где и `_streamingMessage`, и persisted ASSISTANT присутствуют одновременно.

## TDD Anchor

Тесты пишутся ДО реализации. Запускаем → убеждаемся что падают → пишем код → убеждаемся что проходят.

Файл: `app/src/test/kotlin/app/sanctum/machina/ui/chat/ChatViewModelTest.kt`

- `quickMode_neverCallsMessageDao` — создать VM в Quick режиме, отправить сообщение, убедиться что `MessageDao.observeByChat` и `MessageDao.insert` не вызываются ни разу.
- `draftMode_firstSend_callsCommitDraftChat` — создать VM в Draft режиме, вызвать `send(text)`, убедиться что `chatRepository.commitDraftChat(...)` был вызван.
- `draftMode_afterCommit_navigatesToPersistentRoute` — после успешного `commitDraftChat` проверить что `navController.navigate("chat/{id}", popUpTo draft inclusive=true)` был вызван.
- `onCleared_doesNotCallRegistryCleanup` — создать VM, вызвать `onCleared()` через рефлексию или `viewModel.clear()`, убедиться что `registry.cleanup(...)` не вызывался.
- `engineStateTransition_readyToLoading` — `registry.models` эмитит `Initializing` → `uiState` переходит в `Loading`.
- `engineStateTransition_initializingToReady` — `registry.models` эмитит `Ready` → `uiState` переходит в `Ready(isGenerating=false)`.
- `engineStateTransition_failedState` — `registry.models` эмитит `Failed(msg)` → `uiState` переходит в `Failed(msg)`.
- `doubleBubble_noSimultaneousStreamingAndPersisted` — при `done=true`: `persistedMessages` эмитит список с новой ASSISTANT-строкой; убедиться что ни один следующий emission из `displayMessages` не содержит одновременно persisted ASSISTANT и non-null `streamingMessage`.

## Acceptance Criteria

- [ ] `ChatIdentity` sealed class (`Quick`, `Draft`, `Persistent(id: Long)`) существует и читается из `SavedStateHandle`.
- [ ] `ChatViewModel.init {}` не вызывает `registry.initialize(...)` — только `registry.models.collect {}`.
- [ ] `uiState` реактивно отражает `ModelInitStatus`: `Ready` → `ChatUiState.Ready`, `Initializing` → `Loading`, `Failed` → `ChatUiState.Failed`, `Idle` → `Loading`.
- [ ] Для `Persistent` режима: `displayMessages` = `combine(persistedMessages, _streamingMessage)`, где `persistedMessages` — из `messageDao.observeByChat(chatId)`.
- [ ] Для `Quick` и `Draft` режимов: `MessageDao` не вызывается ни разу.
- [ ] Atomic handover: в момент `done=true` `_streamingMessage` сбрасывается в том же emission-цикле где `persistedMessages` впервые содержит новую ASSISTANT-строку — двойной bubble не возникает.
- [ ] Draft→Persistent: первый `send()` в Draft вызывает `chatRepository.commitDraftChat(...)`, после успеха навигирует на `chat/{chatId}` с `popUpTo("chat/draft"){inclusive=true}`.
- [ ] `ChatViewModel.onCleared()` не содержит вызова `registry.cleanup(...)` (AC-E6, Decision 5).
- [ ] `./gradlew :app:testDebugUnitTest` — все тесты зелёные, включая `ChatViewModelTest`.
- [ ] AC-E4: `uiState` начинается с `ChatUiState.Loading` при открытии, без блокировки UI.
- [ ] AC-R1: USER-сообщение пишется в Room (через `ChatRepository`) синхронно до вызова `helper.runInference`.
- [ ] AC-R2: ASSISTANT-сообщение пишется в Room только на `done=true`.
- [ ] AC-R3: при kill посреди стрима USER в Room присутствует, ASSISTANT — нет.

## Context Files

**Feature:**
- [user-spec.md](../user-spec.md)
- [tech-spec.md](../tech-spec.md)
- [decisions.md](../decisions.md)

**Project knowledge:**
- [project.md](../../.claude/skills/project-knowledge/references/project.md)
- [architecture.md](../../.claude/skills/project-knowledge/references/architecture.md)
- [patterns.md](../../.claude/skills/project-knowledge/references/patterns.md)

**Code files to modify:**
- [ChatViewModel.kt](../../../app/src/main/kotlin/app/sanctum/machina/ui/chat/ChatViewModel.kt) — текущая реализация, полностью переписывается

**Code files to read (dependencies из Task 4, 5, 6):**
- [ChatRepository.kt](../../../app/src/main/kotlin/app/sanctum/machina/data/ChatRepository.kt) — интерфейс `commitDraftChat`, `savePersistentMessage` и т.д.
- [WarmupCoordinator.kt](../../../app/src/main/kotlin/app/sanctum/machina/engine/WarmupCoordinator.kt) — `cancelAndRestart(modelId)` API
- [DefaultModelRegistry.kt](../../../core-runtime/src/main/kotlin/app/sanctum/machina/core/registry/DefaultModelRegistry.kt) — `models: StateFlow<List<ModelEntry>>`, `activeModelName: StateFlow<String?>`

**Code file to create:**
- [ChatViewModelTest.kt](../../../app/src/test/kotlin/app/sanctum/machina/ui/chat/ChatViewModelTest.kt) — unit tests

## Verification Steps

### Automated

- `./gradlew :app:testDebugUnitTest --tests "app.sanctum.machina.ui.chat.ChatViewModelTest" -i` → все тесты зелёные
- `./gradlew :app:testDebugUnitTest` → полный прогон unit-тестов `:app`, никаких регрессий

## Details

### Files

**`app/src/main/kotlin/app/sanctum/machina/ui/chat/ChatViewModel.kt`** — полная переработка:

*Текущее состояние:*
- `modelName: String` читается из `SavedStateHandle["modelName"]`; выбрасывает `error()` если отсутствует.
- `init {}` запускает `applyEffectiveConfigToModel()` затем `registry.initialize(modelName)` — imperative, блокирует на 5–30 с.
- `_messages: MutableStateFlow<List<Message>>` — всё in-memory, теряется при смерти процесса.
- `onCleared()` — `CoroutineScope(SupervisorJob() + Dispatchers.IO).launch { registry.cleanup(modelName) }` — **убивает прогретый движок**.
- `NAV_ARG_MODEL_NAME = "modelName"` — устаревший nav arg.

*Что изменить:*
- Добавить `ChatIdentity` sealed class рядом или в отдельном файле того же пакета.
- Заменить `modelName` поле на `chatIdentity: ChatIdentity`; `modelName: String` теперь вычисляемое (`chatIdentity` → DAO read для Persistent, defaultModel для Quick/Draft).
- `_persistedMessages: MutableStateFlow<List<MessageEntity>>` + `_streamingMessage: MutableStateFlow<Message?>`.
- `displayMessages: StateFlow<List<Message>>` = `combine(_persistedMessages, _streamingMessage) { persisted, streaming -> persisted.map { it.toDomain() } + listOfNotNull(streaming) }.stateIn(viewModelScope, SharingStarted.Eagerly, emptyList())`.
- `init {}` запускает два корутина параллельно: (1) `registry.models.collect {}` для engine state; (2) для Persistent — `messageDao.observeByChat(chatId).collect { _persistedMessages.value = it }`.
- `applyEffectiveConfigToModel()` остаётся в init, но вызывается до начала collect-цикла.
- В `send()` для Persistent: (а) вызвать `chatRepository.writeAttachmentsStaging(attachments)` → staging dir; (б) `chatRepository.commitDraftChat(...)` для Draft или `chatRepository.savePersistentMessage(USER, text, ...)` для уже-Persistent; (в) вызвать `helper.runInference(...)`.
- На `done=true` в resultListener: вызвать `chatRepository.savePersistentMessage(ASSISTANT, text, ...)` → после успешного Room INSERT Room Flow эмитит → VM в `mapLatest` сбрасывает `_streamingMessage = null`.
- `onCleared()` — удалить cleanup call. Если нет другой логики — удалить метод вовсе.

**`app/src/test/kotlin/app/sanctum/machina/ui/chat/ChatViewModelTest.kt`** — создать:

Использовать `kotlinx-coroutines-test` + `TestCoroutineScheduler` + ручные fakes:
- `FakeModelRegistry` — `MutableStateFlow<List<ModelEntry>>`, управляемый из теста.
- `FakeMessageDao` — записывает вызовы, возвращает управляемый Flow.
- `FakeChatRepository` — записывает вызовы `commitDraftChat`, `savePersistentMessage`.
- `FakeNavController` — записывает `navigate(...)` вызовы.
- Паттерн тестирования ViewModel с `viewModelScope` — передавать `TestDispatcher` через `Dispatchers.setMain(testDispatcher)` в `@Before` / `@After`.

### Dependencies

- Задача 4 (ChatRepository): `commitDraftChat(modelId, firstMessage, stagingDir)`, `savePersistentMessage(chatId, role, text, ...)`, `observeMessages(chatId)`.
- Задача 5 (WarmupCoordinator): `cancelAndRestart(modelId: String)` — вызывается из `applyHeavySetting` (cross-model reinit в TopAppBar).
- Задача 6 (SanctumApplication): `WarmupCoordinator` уже запущен к моменту открытия чата — VM не должен запускать warmup сам.
- Задача 7 (Navigation): nav args `chatId: Long`, `kind: String` — VM читает из `SavedStateHandle`.
- `MessageDao.observeByChat(chatId: Long): Flow<List<MessageEntity>>` — из Task 1.
- `activeModelName: StateFlow<String?>` на `ModelRegistry` — из Task 3.

### Edge cases

- **Draft + Quick mode**: `chatIdentity` не содержит `chatId` → `messageDao.observeByChat(...)` не вызывается; `_persistedMessages` остаётся пустым.
- **Persistent, process death mid-stream**: USER уже в Room (записан до `runInference`), ASSISTANT не записан (done=true не наступил) → при переоткрытии Room возвращает только USER, пользователь видит незавершённый разговор и может перезадать (AC-R3).
- **Rotation во время Draft send**: если `commitDraftChat` ещё не завершился и процесс ротируется — `SavedStateHandle` содержит ещё `kind=draft`; VM пересоздаётся в Draft режиме. Текст и вложения должны быть в `SavedStateHandle` или в памяти ViewModel (ViewModels переживают rotation).
- **Double-bubble race**: persisted ASSISTANT row приходит из Room, но `_streamingMessage` ещё не null → нужно сбросить `_streamingMessage` атомарно в том же `transformLatest` блоке, не дожидаясь следующего emit.
- **`registry.models` emit перед тем как Room-DAO готов** (для Persistent): VM должен быть в Loading пока не получит и engine Ready, и начальный snapshot из Room.
- **`applyEffectiveConfigToModel()` и модель ещё не в registry**: метод вызывает `preInitModel()` который может вернуть null → silent no-op, продолжаем. Initialize-вызов теперь не наш — WarmupCoordinator разберётся.
- **`send()` в Draft режиме при `reinitInProgress`**: блокируем как и прежде — если идёт cross-model reinit через TopAppBar, Send недоступен.

### Implementation hints

- `ChatIdentity` читается из `SavedStateHandle` в `init` — не в ленивом свойстве, чтобы ошибка "nav arg missing" всплывала сразу.
- `combine(flow1, flow2) { a, b -> ... }` из `kotlinx.coroutines.flow` — для `displayMessages`.
- Для atomic handover: внутри `mapLatest { (persisted, streaming) -> ... }` — если `streaming != null` и `persisted` содержит новую ASSISTANT-строку → вернуть `persisted.map { it.toDomain() }` без streaming (и обновить `_streamingMessage = null` side-effectом). Это гарантирует что единственный emission содержит либо streaming, либо persisted, но не оба.
- Тестирование `onCleared`: использовать `viewModel.clear()` (доступен через `AbstractSavedStateViewModelFactory` test API) или рефлексию → проверить через `FakeModelRegistry.cleanupCalls.isEmpty()`.
- Паттерн: `_uiState.update { current -> when (status) { ... } }` внутри `collect {}` — не создавать новый `Loading` state если текущий уже `Loading` (избежать лишних recompositions).
- `viewModelScope` использует `Dispatchers.Main.immediate` по умолчанию — в тестах подменяй через `Dispatchers.setMain(UnconfinedTestDispatcher())`.
- `SharingStarted.Eagerly` для `displayMessages.stateIn(...)` — важно для того чтобы combine начал работу немедленно при создании VM, а не при первом subscriber.

## Reviewers

- **code-reviewer** → `work/phase-3-history/logs/working/task-8/code-reviewer-{round}.json`
- **security-auditor** → `work/phase-3-history/logs/working/task-8/security-auditor-{round}.json`
- **test-reviewer** → `work/phase-3-history/logs/working/task-8/test-reviewer-{round}.json`

## Post-completion

- [ ] Записать краткий отчёт в decisions.md по шаблону (Summary: 1-3 предложения, ревью со ссылками на JSON, без таблиц файндингов и дампов)
- [ ] Если отклонились от спека — описать отклонение и причину
- [ ] Обновить user-spec/tech-spec если что-то изменилось
