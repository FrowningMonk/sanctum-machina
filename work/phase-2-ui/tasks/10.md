---
status: planned
depends_on: [9]
wave: 4
skills: [code-writing]
verify: [smoke, user]
reviewers: [code-reviewer, security-auditor, test-reviewer]
teammate_name:
---

# Task 10: MessageBubble extraction + ThinkingBlock + markdown rendering + ChatViewModel thinking accumulation + SYSTEM_PROMPT_DEFAULT ConfigKey + engine systemInstruction wiring

## Required Skills

Перед выполнением задачи загрузи:
- `/skill:code-writing` — [skills/code-writing/SKILL.md](~/.claude/skills/code-writing/SKILL.md)

## Description

Задача объединяет несколько связанных изменений, которые вместе формируют полную поддержку thinking-блоков и системного промпта в чате.

1. **MessageBubble extraction** — выносим приватный composable из `ChatScreen.kt` в отдельный файл `MessageBubble.kt` для чистоты архитектуры.
2. **ThinkingBlock composable** — новый collapsible компонент с визуальной левой линией (`drawBehind(outlineVariant)`), приглушённым текстом, `SafeMarkdown` внутри и авто-раскрытием пока идёт генерация.
3. **Message.thinkingText** — добавляем поле `thinkingText: String?` в data-класс `Message`.
4. **ChatViewModel thinking accumulation** — `resultListener` принимает 3-й аргумент (thinking chunk), накапливает в отдельном StringBuilder, пропускает если `!llmSupportThinking || !enableThinking`.
5. **ConfigKeys.SYSTEM_PROMPT_DEFAULT** — новый ConfigKey в `Config.kt`, расширяем `createLlmChatConfigs` параметром `defaultSystemPrompt`.
6. **awaitInitialize wiring (D24)** — `DefaultModelRegistry.awaitInitialize` пробрасывает `systemInstruction` в `engine.createConversation` на основе значения из `model.configValues[SYSTEM_PROMPT_DEFAULT]`.

## What to do

1. Прочитать `gallery-source/Android/src/app/src/main/java/com/google/ai/edge/gallery/ui/common/chat/MessageBodyThinking.kt` — понять оригинальный паттерн thinking блока.
2. Прочитать `core-runtime/src/main/kotlin/app/sanctum/machina/core/registry/DefaultModelRegistry.kt` — понять текущий поток инициализации.
3. Добавить `thinkingText: String?` в `Message.kt`.
4. Создать `ThinkingBlock.kt` — collapsible composable с левой линией, мутный текст, `SafeMarkdown(thinkingText)`, auto-expand при `inProgress = true`.
5. Создать `MessageBubble.kt` — вынести composable из `ChatScreen.kt`; внутри: если `thinkingText != null && model.llmSupportThinking` → рендерить `ThinkingBlock` над основным `SafeMarkdown(text)`.
6. Удалить приватный composable из `ChatScreen.kt`, заменить вызовом `MessageBubble`.
7. В `ChatViewModel` — расширить `resultListener` накоплением thinking в отдельный StringBuilder; при обновлении state включать `thinkingText`; guard: пропустить накопление если `!model.llmSupportThinking || !effectiveConfig[ENABLE_THINKING]`.
8. В `Config.kt` — добавить `ConfigKeys.SYSTEM_PROMPT_DEFAULT` (String, default `""`).
9. В `ModelAllowlist.kt` — парсить `defaultSystemPrompt` из JSON, передавать в `createLlmChatConfigs`.
10. В `Config.kt` / `createLlmChatConfigs` — добавить параметр `defaultSystemPrompt: String = ""`, эмитировать как default value ConfigKey.
11. В `DefaultModelRegistry.awaitInitialize` — извлекать `model.configValues[ConfigKeys.SYSTEM_PROMPT_DEFAULT.label]`, строить `systemInstruction: Contents?`, передавать в `llmModelHelper.initialize(...)`.
12. Добавить строки локализации в `strings.xml` (например, "Thinking", "Show thinking", "Hide thinking").

## TDD Anchor

- `:core-runtime` unit-тест: `createLlmChatConfigs` с непустым `defaultSystemPrompt` → конфиг содержит `SYSTEM_PROMPT_DEFAULT` с ожидаемым значением.
- `:core-runtime` unit-тест: `createLlmChatConfigs` с пустым `defaultSystemPrompt` → конфиг содержит `SYSTEM_PROMPT_DEFAULT` равный `""`.
- `:core-runtime` unit-тест: `awaitInitialize` с непустым `SYSTEM_PROMPT_DEFAULT` в `configValues` → `systemInstruction` передаётся не-null.
- UI unit-тест (опционально): `ThinkingBlock` при `inProgress = true` рендерится раскрытым; при `inProgress = false` — свёрнутым.

## Acceptance Criteria

- [ ] `MessageBubble` extracted to separate file from ChatScreen
- [ ] `ThinkingBlock` is collapsible with left outline line, muted text, SafeMarkdown
- [ ] ThinkingBlock auto-expands when `inProgress`, collapsible when done
- [ ] `Message.thinkingText: String?` field added
- [ ] ChatViewModel accumulates thinking from resultListener 3rd arg
- [ ] Thinking skipped when `!llmSupportThinking || !enableThinking`
- [ ] `ConfigKeys.SYSTEM_PROMPT_DEFAULT` added to Config.kt
- [ ] `createLlmChatConfigs` extended with `defaultSystemPrompt` parameter
- [ ] `DefaultModelRegistry.awaitInitialize` passes systemInstruction to engine (D24)
- [ ] `ModelAllowlist` parses `defaultSystemPrompt` and passes to `createLlmChatConfigs`
- [ ] Existing core-runtime tests remain green

## Context Files

- [user-spec.md](../user-spec.md)
- [tech-spec.md](../tech-spec.md)
- [decisions.md](../decisions.md)
- [architecture.md](~/.claude/skills/project-knowledge/architecture.md)
- [patterns.md](~/.claude/skills/project-knowledge/patterns.md)
- [project.md](~/.claude/skills/project-knowledge/project.md)
- [testing.md](~/.claude/skills/project-knowledge/testing.md)
- `gallery-source/Android/src/app/src/main/java/com/google/ai/edge/gallery/ui/common/chat/MessageBodyThinking.kt` — оригинальный thinking блок для референса
- `core-runtime/src/main/kotlin/app/sanctum/machina/core/registry/DefaultModelRegistry.kt` — точка подключения systemInstruction

## Verification Steps

### Automated

- `./gradlew :core-runtime:test` → all pass (зелёный)
- `./gradlew :app:assembleDebug` → BUILD SUCCESS

### Smoke

- `./gradlew :core-runtime:test` — убедиться что все unit-тесты зелёные
- `./gradlew :app:assembleDebug` — убедиться что APK собирается без ошибок

### User

- Установить APK на Honor 200
- Открыть ChatScreen с моделью, поддерживающей thinking (`llmSupportThinking = true`)
- Включить `enableThinking` через `resetConversation`
- Убедиться, что thinking-блок отображается над основным текстом (AC-7)
- Убедиться, что thinking-блок collapsible после завершения генерации (AC-14)
- Убедиться, что thinking-блок авто-раскрыт во время генерации (AC-18)
- Настроить `SYSTEM_PROMPT_DEFAULT` для модели → проверить что системный промпт применяется в разговоре (AC-4)

## Details

**Files to modify:**
- `app/src/main/kotlin/app/sanctum/machina/ui/chat/ChatScreen.kt` — удалить приватный MessageBubble composable, импортировать из нового файла
- `app/src/main/kotlin/app/sanctum/machina/ui/chat/Message.kt` — добавить `thinkingText: String?`
- `app/src/main/kotlin/app/sanctum/machina/ui/chat/ChatViewModel.kt` — накопление thinking в resultListener, guard по флагам
- `app/src/main/res/values/strings.xml` — строки для ThinkingBlock UI ("Thinking", "Show thinking" / "Hide thinking")
- `core-runtime/src/main/kotlin/app/sanctum/machina/core/data/Config.kt` — добавить `SYSTEM_PROMPT_DEFAULT` ConfigKey, расширить `createLlmChatConfigs`
- `core-runtime/src/main/kotlin/app/sanctum/machina/core/registry/DefaultModelRegistry.kt` — systemInstruction wiring в awaitInitialize
- `core-runtime/src/main/kotlin/app/sanctum/machina/core/data/ModelAllowlist.kt` — парсить и передавать `defaultSystemPrompt`

**Files to create:**
- `app/src/main/kotlin/app/sanctum/machina/ui/chat/MessageBubble.kt` — extracted composable
- `app/src/main/kotlin/app/sanctum/machina/ui/chat/ThinkingBlock.kt` — collapsible thinking block

**Files to read before starting:**
- `gallery-source/Android/src/app/src/main/java/com/google/ai/edge/gallery/ui/common/chat/MessageBodyThinking.kt`
- `core-runtime/src/main/kotlin/app/sanctum/machina/core/registry/DefaultModelRegistry.kt`

**Tech-spec decisions:** D9 (thinking field), D10 (autoscroll), D24 (systemPromptDefault plumbing)

**awaitInitialize wiring (D24) — key code:**
```kotlin
// DefaultModelRegistry.awaitInitialize — NEW systemInstruction plumbing
val systemPrompt = (model.configValues[ConfigKeys.SYSTEM_PROMPT_DEFAULT.label] as? String)
  ?.takeIf { it.isNotBlank() }
val systemInstruction: Contents? = systemPrompt?.let { Contents.of(listOf(Content.Text(it))) }
llmModelHelper.initialize(
  ...,
  systemInstruction = systemInstruction,   // was effectively null
  ...
)
```

**Edge cases:**
- `thinkingText` может быть `null` (модель не поддерживает thinking или флаг выключен) — ThinkingBlock не рендерится
- `systemPrompt` пустой или blank — `systemInstruction` должен быть `null`, не пустой объект
- Смена модели или `resetConversation` — thinking StringBuilder должен сбрасываться
- Collapsible state ThinkingBlock: `inProgress = true` → принудительно раскрыт, нельзя свернуть; после завершения — toggle доступен

**Implementation hints:**
- Левая линия ThinkingBlock: `Modifier.drawBehind { ... }` рисует линию цвета `MaterialTheme.colorScheme.outlineVariant`
- Мутный текст: `LocalContentAlpha` или `contentAlpha = 0.6f` / `color = MaterialTheme.colorScheme.onSurfaceVariant`
- `SafeMarkdown` — создаётся в Task 6 (зависимость: Task 10 → 9 → 8 → 7 → 6, поэтому доступен). Не существует до выполнения Task 6
- ThinkingBlock состояние: `var expanded by remember { mutableStateOf(inProgress) }` + `LaunchedEffect(inProgress) { if (inProgress) expanded = true }`
- resultListener 3-й аргумент — убедиться в сигнатуре колбека по исходнику gallery

## Reviewers

- **code-reviewer** → `work/phase-2-ui/logs/working/task-10/code-reviewer-{round}.json`
- **security-auditor** → `work/phase-2-ui/logs/working/task-10/security-auditor-{round}.json`
- **test-reviewer** → `work/phase-2-ui/logs/working/task-10/test-reviewer-{round}.json`

## Post-completion

- [ ] Записать краткий отчёт в decisions.md по шаблону (Summary: 1-3 предложения, ревью со ссылками на JSON, без таблиц файндингов и дампов)
- [ ] Если отклонились от спека — описать отклонение и причину
- [ ] Обновить user-spec/tech-spec если что-то изменилось
